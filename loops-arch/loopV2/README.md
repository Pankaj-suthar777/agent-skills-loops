# loopV2

Work in progress — this folder is currently empty.

Next version of the engineering loop, planned to replace or extend `loopV1`. Once populated, it will follow the same structure:

```
loopV2/
├── agents/           # orchestrator, worker, tester, reviewer
├── loop-state/       # current.json
├── loop-history.md
└── <agent-CLI config>
```

See `loopV1/README.md` and the root `README.md` for how the loop works.
